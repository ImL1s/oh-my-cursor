# Accept

Load and follow the `accept` skill from Oh My Cursor. Treat all text after `/accept` as the workflow input. Use current repository evidence, keep changes scoped, and return the skill's required evidence-backed output.
