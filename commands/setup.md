# Setup

Load and follow the `setup` skill from Oh My Cursor. Treat all text after `/setup` as the workflow input. Use current repository evidence, keep changes scoped, and return the skill's required evidence-backed output.
