# Ask Gemini (Compatibility Advisor)

Query external Gemini compatibility adapter with an advisor prompt. Preserves external provider identity and outputs a typed artifact without editing code.

Usage: `/omcu-ask-gemini <prompt>`
CLI route: `omcu ask gemini --prompt "<prompt>"`
