# Ralplan

Load and follow the `ralplan` skill from Oh My Cursor. Treat all text after `/ralplan` as the workflow input. Use current repository evidence, keep changes scoped, and return the skill's required evidence-backed output.
