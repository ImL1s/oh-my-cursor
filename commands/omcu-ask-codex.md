# Ask Codex (Compatibility Advisor)

Query external Codex compatibility adapter with an advisor prompt. Preserves external provider identity and outputs a typed artifact without editing code.

Usage: `/omcu-ask-codex <prompt>`
CLI route: `omcu ask codex --prompt "<prompt>"`
