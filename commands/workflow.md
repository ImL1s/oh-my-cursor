# Workflow

Load and follow the `workflow` skill from Oh My Cursor. Treat all text after `/workflow` as the workflow input. Use current repository evidence, keep changes scoped, and return the skill's required evidence-backed output.
