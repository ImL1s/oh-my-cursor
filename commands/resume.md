# Resume

Load and follow the `resume` skill from Oh My Cursor. Treat all text after `/resume` as the workflow input. Use current repository evidence, keep changes scoped, and return the skill's required evidence-backed output.
