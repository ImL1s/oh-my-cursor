# Provider Compare & Consensus (Cursor, Codex, Gemini)

Runs multi-provider compare and consensus across Cursor, Codex, and Gemini. Produces a typed advisory consensus artifact under `.omcu/artifacts/` without editing repository code.

Usage: `/omcu-ccg <prompt>`
CLI route: `omcu ask --compare cursor,codex,gemini --prompt "<prompt>"`
