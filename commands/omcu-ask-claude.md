# Ask Claude (Compatibility Advisor)

Query external Claude compatibility adapter with an advisor prompt. Preserves external provider identity and outputs a typed artifact without editing code.

Usage: `/omcu-ask-claude <prompt>`
CLI route: `omcu ask claude --prompt "<prompt>"`
