# Ralph

Load and follow the `ralph` skill from Oh My Cursor. Treat all text after `/ralph` as the workflow input. Use current repository evidence, keep changes scoped, and return the skill's required evidence-backed output.
