# Recover

Load and follow the `recover` skill from Oh My Cursor. Treat all text after `/recover` as the workflow input. Use current repository evidence, keep changes scoped, and return the skill's required evidence-backed output.
