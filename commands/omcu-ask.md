# Ask (Cursor Advisor)

Query Cursor canonical model runtime with an advisor prompt. Output is a typed artifact and does not edit code unless explicitly launched as a worker.

Usage: `/omcu-ask <prompt>`
CLI route: `omcu ask cursor --prompt "<prompt>"`
