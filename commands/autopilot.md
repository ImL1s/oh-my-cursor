# Autopilot

Load and follow the `autopilot` skill from Oh My Cursor. Treat all text after `/autopilot` as the workflow input. Use current repository evidence, keep changes scoped, and return the skill's required evidence-backed output.
